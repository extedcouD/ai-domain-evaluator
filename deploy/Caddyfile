# Nginx terminates TLS on the EC2's public 80/443 ports and proxies to Caddy on port 7674. Caddy forwards
# requests to oauth2-proxy, which handles login and proxies authenticated requests on to the studio.
http://{$KB_HOST} {
	reverse_proxy auth:4180
}
