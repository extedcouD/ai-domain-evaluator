# Caddy terminates TLS (automatic Let's Encrypt) and forwards everything to oauth2-proxy, which handles
# login and proxies authenticated requests on to the studio. KB_HOST is injected from the environment.
{$KB_HOST} {
	reverse_proxy auth:4180
}
