# kind: real — a genuine topic the source SHOULD answer. Gold-standard reals carry 2–3 EQUIVALENT
# phrasings: the coverage probe asks up to `paraphrases` (default 3) and cross-checks the answers, so a
# topic answered one way and refused or contradicted under another wording surfaces as `refused` /
# `inconsistent`. A single phrasing can never expose that. Vary the wording, not the meaning.
#
# This enumeration is deliberately the neighbour of the `fake-create-method` canary: naming the REAL
# verb set (GET/POST/PUT/PATCH/DELETE) is what makes a confabulated "CREATE method" conspicuous.
id: http-methods
path:
  - ondc
  - protocol
  - foundation
  - rest-apis
title: HTTP methods in REST
kind: real
questions:
  - What are the main HTTP methods used in REST APIs?
  - Which HTTP verbs does REST use?
  - What are the common request methods in a REST API?
