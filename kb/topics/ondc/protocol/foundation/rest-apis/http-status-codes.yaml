# kind: real — a genuine topic the source SHOULD answer. Gold-standard reals carry 2–3 EQUIVALENT
# phrasings: the coverage probe asks up to `paraphrases` (default 3) and cross-checks the answers, so a
# topic answered one way and refused or contradicted under another wording surfaces as `refused` /
# `inconsistent`. A single phrasing can never expose that. Vary the wording, not the meaning.
#
# This enumeration is the neighbour of the `fake-partial-success-status` canary: naming the REAL code
# families (2xx/3xx/4xx/5xx) is what makes a confabulated "250 Partial Success" conspicuous.
id: http-status-codes
path:
  - ondc
  - protocol
  - foundation
  - rest-apis
title: HTTP status codes
kind: real
questions:
  - What are HTTP status codes in a REST API?
  - What do the different HTTP response codes mean?
  - How do REST APIs signal success or failure with status codes?
