# kind: real — a genuine topic the source SHOULD answer. Gold-standard reals carry 2–3 EQUIVALENT
# phrasings: the coverage probe asks up to `paraphrases` (default 3) and cross-checks the answers, so a
# topic answered one way and refused or contradicted under another wording surfaces as `refused` /
# `inconsistent`. A single phrasing can never expose that. Vary the wording, not the meaning.
#
# This enumeration is the neighbour of the `fake-beckn-session-id-field` canary: naming the REAL context
# fields (domain, action, bap_id/bap_uri, bpp_id/bpp_uri, transaction_id, message_id, timestamp, ttl…)
# is what makes a confabulated `session_id` field conspicuous.
id: beckn-context-object
path:
  - ondc
  - protocol
  - foundation
  - beckn
title: The Beckn context object
kind: real
questions:
  - What is the context object in a Beckn message?
  - What fields does the Beckn context contain?
  - What information is carried in the context of a Beckn request?
